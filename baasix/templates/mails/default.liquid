<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>{{ subject }}</title>
</head>
<body style="font-family: 'Helvetica Neue', 'Helvetica', 'Arial', sans-serif; font-size: 16px; line-height: 1.6; color: #333; background-color: #f8f8f8; margin: 0; padding: 20px;">
    <div style="max-width: 600px; margin: 0 auto; background-color: #fff; border: 1px solid #f0f0f0; border-radius: 5px; box-shadow: 0px 0px 10px 8px #b7b1b1;">
        <header style="text-align: center; padding: 20px; background-color:{{project_color |  default: '#0000ff'}};">
            <img src="{{ logo_url }}" alt="Logo" style="max-width: 200px;">
        </header>
        <main>
            <div style="padding: 20px;">
                {{ content }}
                {{ email_signature }}
            </div>
        </main>
        <footer style="text-align: center; padding: 20px; font-size: 12px; color: #888;">
            © {{ "now" | date: "%Y" }} {{ project_name }}. All rights reserved.
        </footer>
    </div>
</body>
</html>